import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Phase 8 placeholder — full configuration will be added in Phase 8.
export default defineConfig({
  plugins: [react()],
});
